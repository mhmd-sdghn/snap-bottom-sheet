# Normalize line endings to LF in the working tree on every platform, so the
# Biome formatter (lineEnding: "lf") and `pnpm lint` stay green on Windows
# checkouts where core.autocrlf would otherwise produce CRLF.
* text=auto eol=lf
