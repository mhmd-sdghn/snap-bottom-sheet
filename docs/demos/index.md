# Demos

Live demos are added in a follow-up (task 09). The frame and wiring they need
are already in place, so each demo is a small React component plus one line on
a page.

Planned:

| Demo | Shows |
| --- | --- |
| Basic | A single sheet, content mode, drag to dismiss |
| Snap points | Three snaps, `defaultSnapIndex`, `onSnapIndexChange` |
| Dynamic height | `"header"` and `"content"`, re-measured live |
| Scrollable list | `{ value: 1, scroll: true }` and the scroll-vs-drag handover |
| Nested | A sheet opened from inside a sheet, Escape to the innermost |
| Vanilla | `createSheet` against hand-written markup |

Until then, every page in the [Guide](/guide/getting-started) carries complete,
copy-pasteable code.
